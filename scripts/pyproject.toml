[project]
name = "vortex-dataset-scripts"
version = "0.1.0"
description = "Dataset conversion and synchronization scripts for Vortex"
requires-python = ">=3.11"
dependencies = [
    "hf-xet>=1.1.10",
    "httpx>=0.27",
    "huggingface-hub>=0.34.0",
    "pyarrow>=17.0.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["ruff>=0.12.0"]
